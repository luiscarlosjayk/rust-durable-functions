import type { Environment } from "../../types/environment";

const environment: Environment = {
    orgName: "luisk",
    envName: "dev",
    appName: "rust-durable-functions",
    region: "us-east-1",
    provisionedConcurrencyEnabled: false,
    logLevel: "debug",
};

export default environment;
